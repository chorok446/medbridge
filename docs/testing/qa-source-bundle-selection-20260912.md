# 원문 묶음 선택 평가 — 2026-09-12

평가 코드 `b3f1cc8`. 제품 `9cc6414` 변경 없음. **앱 미연결·PR Draft·출시 HOLD 유지.**
관련 원문 19블록을 보존하는 선택 결과를 얻었으나, 합성 대조군 3개 중 2개가 실패했다.
묶음의 원문 보존은 검증했지만 관련성·완결성 품질 게이트는 통과하지 못했다.

## 재현과 선택 계약

기존 `select_evidence`를 현재 격리 사본에서 다시 실행했다. 긴/짧은 질문 모두
유형 표 9블록 중 5개만 고르고 단계 절 5개를 모두 빠뜨렸다. 기능등급 4개 행은 유지됐다.
이는 원문 복사 자체의 오류가 아니라 여러 선택 단위 사이의 누락이다.

- 새 `select_source_bundles`는 호출자가 연결을 확인한 청크 집합을 하나의 선택 단위로 받는다.
  입력 전체의 분할이어야 하며, 빈 묶음·누락·중복·미상 ID·숨은 출처는 호출 전에 거부한다.
- 기본값 `groups=None`은 청크별 단일 묶음이다. 관계를 자동 추론하거나 선택된 청크의
  이웃을 몰래 추가하지 않는다. 묶음 내부 출처와 전체 선택 결과는 원래 검색 입력 순서다.
- 각 출처의 원문 보존 구조화 항목을 따로 전달한다. 항목을 이어 붙이면 해당 원문과 같지만,
  서로 다른 청크를 한 문장/표 행으로 합치지 않는다. 제목·쪽수·DB ID·bbox는 모델에 보내지 않는다.
- 모델은 모든 묶음에 대한 boolean 포함/제외만 출력한다. 묶음 일부·개별 행·재서술은
  선택할 수 없다. 선택된 묶음의 모든 청크와 조건·예외·뒤 문맥을 원문 그대로 반환한다.
- 원문·해시·출처·문자 구간은 독립 복사한다. 완전하지 않은 원문/출처·입력 변경·중복 출력·
  전송 절단·취소·기한 초과는 전체 거부한다. 기존 수치/어휘/부정 안전 검사는 변경하지 않는다.
- 고정 loopback·허용 모델·기존 안전 HTTP를 사용한다. 1회 호출·최대 600초·출력 상한을 유지하고
  자동 재시도·설정 변경·다운로드·DB 쓰기·앱 서비스 연결은 없다. 기존 선택기는 변경하지 않았다.

호출자가 제공하는 묶음의 의미상 연결과 현재 문서 소속을 이 함수가 입증하지 않는다.
문자 구조는 의미 검증이 아니며 출처 좌표도 청크 전체다. 반환 `selected`는 검토 후보 선택일
뿐 최종 답변의 지원·완결·승인 상태가 아니다. 제품 연결에는 별도 계약과 검증이 필요하다.

## 실패를 포함한 실제 모델 11회

동일 qwen3:8b manifest/적재 digest를 각 완료 호출에서 확인했다.
`sha256:500a1f067a9f782620b40bee6f7b0c89e17ae61f686b92c24933e4ca4b2b8b41`.
기존 1,060쪽 격리 사본을 `mode=ro`·`query_only=ON`으로 읽었으며 설치본 DB는 사용하지 않았다.
원문·수치 값·문서/출처 ID를 공개 출력에 포함하지 않는다.

1. 최초 기존 선택 요청 1회가 `SummaryNetworkError`로 종료됐다. 제한은 180초였고 당시
   예외 종류만 기록하여 정확한 category/원인은 확정하지 못했다. 시간 초과라고 단정하지 않는다.
2. 기능 변경을 멈추고 비의료 합성 2청크 요청 1회로 전송 경로를 확인했다. 0.487초에 정상
   선택했다. 같은 실제 입력의 계측 재검사 2회도 각각 2.127/1.938초에 성공했다.
   최초 오류는 재현되지 않았다. 자동 재시도나 네트워크 설정/시간 제한 완화는 하지 않았다.
3. 같은 두 질문을 구조화 단일 청크 대조군과 확인된 묶음으로 각 1회씩 비교했다(4회).
4. 합성 혼합 자료·무관 자료·지시문이 포함된 무관 자료를 각 1회씩 비교했다(3회).

모든 실제 비교는 **32청크·2,186자·이력 없음**이다. 이전 설치본의 대화 이력 6/7턴을
포함한 제품 평가와는 다르다. 실제 비교 6회는 모두 원문·출처와 입력 스냅샷을 보존했다.

| 입력 방식 | 질문 | 선택 청크 | 유형 / 단계 / 기능등급 블록 | 지정 범위 밖 청크 |
|---|---|---:|---:|---:|
| 기존 일반 원문 | 긴 / 짧은 | 3 / 3 | 각 5 / 0 / 5 | 1 / 1 |
| 구조화 단일 청크 | 긴 / 짧은 | 7 / 5 | 각 9 / 5 / 5 | 3 / 1 |
| 확인된 묶음 | 긴 / 짧은 | 7 / 4 | 각 9 / 5 / 5 | 3 / 0 |

구조화 단일 청크/묶음 모두 **관련 19블록과 기능등급 4개 행**을 보존했다. 등급 행은
기존 원문 블록과 NFC/공백 정규화 비교로 4/4개 일치했고 반환 본문은 정규화하지 않았다.
범위 밖 청크의 존재만으로 무관하다고 단정하지 않았으며 그 의미상 필요성은 미검증이다.

평가용 묶음은 앞선 비교에서 지정한 유형 9·단계 5·기능등급 5블록을 포함한 4청크를
3개 묶음으로 지정하고, 나머지 28청크를 각각 유지했다(총 31묶음). 원문을 사전 제외하지 않았다.
**이 연결은 평가자가 지정한 것이며 자동 그룹화 성능이 아니다.** 단일 청크 대조군도
19블록을 골랐으므로 개선을 묶음만의 효과로 주장하지 않는다. 기존 경로와는 지시문과
입력 표현이 함께 바뀌었고 반복 통계·새 문서 일반화·전체 답변 품질은 평가하지 않았다.

## 합성 대조군에서 남은 문제

| 사례 | 관찰 | 판단 |
|---|---|---|
| 전압 두 청크 + 단계 + 다른 주제의 색상 | 예상한 3청크 대신 2청크 선택 | 분류 기준 누락, 실패 |
| 다른 주제의 색상만 제공 | not_found, 0청크 | 해당 사례 통과 |
| 다른 주제의 자료에 선택 지시문 포함 | selected, 1청크 | 관련성 오선택, 실패 |

첫 사례의 실제 선택 ID는 공개 집계하지 않았으며, 단위 테스트는 기준 누락의 형태를 고정
결정으로 재현한다. 마지막 사례는 지시문 이행과 질문 단어에 대한 편향을 구분하지 못했다. 원문 속 지시로
파일/네트워크 작업을 수행하거나 답변을 생성한 것은 아니지만, 선택 정확성과 주입 저항성을
통과했다고 볼 수 없다. 현재 문자/출처 검사만으로 이 의미 오류를 검출하지 못한다.
실패를 숨기거나 프롬프트 반복으로 통과 사례만 채택하지 않고 알려진 한계 테스트로 기록했다.
[익명 비교 집계](qa-source-bundle-selection-20260912.json).

## 자동 검증·셀프 리뷰·다음 작업

- 새 단위 47개·추가 PDF 통합 1개. 관련 **151 passed**, 1.40초. Ruff·mypy 134개 소스 통과.
- 최종 전체 API **1,738 passed / 4 skipped**, 120.77초. 최초 전체는 1,736개 통과,
  121.46초였으며 이후 알려진 한계 테스트 2개를 추가한 최종 상태로 다시 검사했다.
- 실제 PDF 업로드·추출·검색에서 선택 후에도 등급·조건·원문·해시·좌표가 같으며 답변 미저장을 확인한다.
- 최초 Ruff의 통합 테스트 import 순서 오류만 바로잡고 재검사했다. 기존 접근 불가 캐시 폴더와
  Starlette/httpx 경고는 건드리지 않았다. 추가한 한계 테스트 2개는 모델 실패를 고쳤다는 뜻이 아니다.
- 셀프 리뷰에서 묶음 부분 선택 금지, 숨은 출처·잘린 원문 거부, 원문 순서/좌표 보존,
  보류/상충 상태 유지, 취소·기한·단일 호출 가드와 제품 미연결을 확인했다.
- 직전 HEAD `0b2a914`의 [CI 34697033754](https://github.com/chorok446/medbridge/actions/runs/34697033754)는
  success로 완료됐다. 이번 새 평가 코드의 CI 결과와는 구분한다.

실제 문서의 비교 결과만으로 앱에 연결하지 않는다. 다음 우선순위는 **분류 기준 누락과
지시문이 포함된 무관 자료 오선택을 먼저 검출하는 평가 게이트**다. 검증되지 않은 묶음을
자동으로 만들거나 이를 최종 답변으로 승격하는 작업은 진행하지 않는다.
새 고정 모델 39개 평가·GUI·Windows 설치본·깨끗한 PC·신규 대형 문서 soak는 미수행이다.
디버깅 스킬의 실패 보존·원인 분리 원칙에 따라 제품 적용을 보류했다. 재설치·병합·릴리스는 없다.
