# 전체 청크·같은 질문의 검색 및 생성 비교 — 2026-09-12

> **진단 완료, 제품 코드 변경 없음.** 기준 제품은 `5ef7e14`이다.
> 1,060쪽 전체 사본에서 표/절 연결 개선과 출처 31,401개의 정합성을 확인했다.
> 필요한 분류 원문 19블록은 전후 모두 검색됐다. 생성의 잘못된 수치 출처는 남는다.
> 짧은 질문은 표지 포함이 개선됐지만 긴 질문은 수치 거부 3건으로 불완전하다.
> 사용자 데이터·설치본은 그대로이며 **PR Draft / 출시 HOLD**를 유지한다.

## 비교 방법과 적용 범위

431,634,439바이트·1,060쪽·32,191블록인 익명 문서 `large-1`을 사용했다.
설치 DB를 `mode=ro`, `query_only=ON`으로 열고 SQLite backup으로 격리 사본을 만들었다.
다른 문서의 FTS 통계도 같은 상태에서 시작하고, 대상 문서의 읽기 순서·경계 표시·청크만
사본에서 변경했다. 앱 설치, 실제 PDF 재파싱, 원본 재추출, 사용자 대화 저장은 하지 않았다.

현재 제품 코드의 `rebuild_chunks`와 `qa.context.retrieve`를 그대로 사용했다.
모든 검색은 같은 키워드 모드이며 청크 32개/12,000자/청크당 3,000자 상한을 유지했다.
기존 대화에 있는 긴 분류 질문·짧은 분류 질문·정의 질문을 사용한다. 공개 기록에는
원문·질문 전문·파일명·내부 ID·이력 해시를 넣지 않는다.

| 전체 문서 조건 | 청크 수 | 분류 입력 글자 수 | 기능등급 제목+네 행 |
|---|---:|---:|---|
| 저장된 기존 청크 | 16,239 | 2,250 | 두 청크에 분리 |
| 저장 순서 그대로 청크만 재생성 | 16,239 | 2,250 | 두 청크에 분리 |
| 현재 읽기 순서 보정만 적용 | 16,237 | 2,250 | 한 청크에서 연속 |
| 절 경계 메타데이터까지 적용 | 16,270 | 2,186 | 한 청크에서 연속 |

- 단순 재생성 대조군은 새 청크 UUID를 제외한 16,239개 레코드와 세 질문 입력이 같다.
  실험 도구나 재생성 자체가 비교 결과를 바꾼 것이 아님을 확인했다.
- 전체 읽기 순서 재계산은 **21쪽·82블록**의 순서를 바꿨다. 배치 조건을 통과한
  절 경계 표시 **37개**를 추가했다. 37개 모두의 의미상 제목 정확성을 수동 검증한 것은 아니다.
- 저신뢰 제외 블록은 모든 재생성에서 **15개**로 같다.
- 분류 질문 두 개는 모든 조건에서 같은 32개 입력을 얻었다. 청크별 절단은 없다.
- 정의 질문 대조군은 세 전환 모두 **동일한 원문·출처·순서 입력**, 2,090자를 유지한다.
- 위 시간/개수는 단일 실행 관측치이며 신규 대형 PDF 업로드·메모리 soak 검증이 아니다.

## 검색 근거와 전체 출처 확인

분류 관련 유형 표 9블록, 단계 절 5블록, 기능등급 표 5블록을 확인했다.
**19블록 모두 수정 전후 검색된 청크의 모델 가시 텍스트에 온전히 있었다.**
원문 블록 ID의 참조 존재뿐 아니라 해당 블록의 정규화 텍스트 포함도 확인했다.
HFrEF/HFpEF/NYHA 표지도 모든 분류 검색에서 유지됐다.

따라서 이번 관측에서 원문 부재나 12,000자 절단을 답변 생략의 원인으로 볼 수 없다.
묶임의 개선과 최종 답변의 완결성은 별개다.

- 이전 표 내용과 새 단계 절이 같은 청크에 남던 현상은 사본의 전체 청크에서도 해소됐다.
  절 제목과 네 항목은 순서대로 붙었다. 다만 해당 청크에는 후속 본문도 있어 총 출처가
  **12개**다. 모든 절을 완전히 독립된 청크로 만들었다는 뜻은 아니다.
- 기능등급 표 제목과 네 행은 한 청크의 출처 5개로 연속 연결됐다.
- 유형 표의 9블록은 여전히 두 청크다. 둘 모두 모델 입력에 들어간다.
- 최종 사본에서 선택된 원문 **31,401블록**과 청크 출처 **31,401개**가 일치했다.
  누락·예상 밖 블록·bbox/읽기 순서/쪽수/디지털·OCR 출처 불일치는 모두 **0건**이다.
- 사본의 페이지, 원문·좌표·원래 인덱스, 다른 문서 블록, 설정과 대화 해시가 유지됐다.
  사본 quick_check 정상, FK 위반 0건이다.

## 로컬 모델 네 번 비교

긴/짧은 질문 각각 저장 청크와 수정 사본을 한 번씩 실행했다. 현재 제품 공급자·프롬프트·
검증·최종 상태 계산을 사용하며 실제 GUI 작업자가 저장하는 대화 경로는 호출하지 않았다.
원래 질문 전의 동일 이력 6/7턴, 같은 qwen3:8b와 모델 digest를 유지했다.
각 응답 후 적재 모델 digest도 확인했다. 모델 비교는 앞선 읽기 순서 수정까지 합친
누적 데이터 변경 점검이며, 절 표시 하나만의 인과 효과를 분리한 실험은 아니다.

| 질문·청크 조건 | 생성 / 지원 주장 | 수치 출처 거부 | 지원 문장의 표지 | 시간 / 요청 시도 |
|---|---:|---:|---|---:|
| 긴 질문·기존 | 3 / 3 | 0 | HFrEF, HFpEF | 19.347초 / 1 |
| 긴 질문·수정 사본 | 5 / 2 | 3 | NYHA | 60.843초 / 3 |
| 짧은 질문·기존 | 3 / 3 | 0 | HFrEF, HFpEF | 22.221초 / 2 |
| 짧은 질문·수정 사본 | 5 / 4 | 1 | HFrEF, HFpEF, NYHA | 16.532초 / 1 |

네 응답 모두 최종 `completed`였지만 이것을 완전한 답변으로 평가하지 않는다.
수정 사본에서는 두 질문 모두 생성 문장에 세 표지가 나타났다. 긴 질문은 유형 관련
표지를 포함한 주장이 거부되어 지원 결과에서 빠졌고, 짧은 질문도 수치 거부 1건이 남았다.
표지 포함과 어휘 검사 통과는 모든 분류 항목·조건의 정확한 설명을 보장하지 않는다.

수치 거부를 원문 비노출 집계로 확인한 결과:

- 긴 질문의 세 거부 주장: 인용 청크에 없는 수치 토큰이 각각 **4/6/2개**였다.
  해당 토큰 **4/6/2개 모두 다른 검색 청크에는 존재**했다.
- 짧은 질문의 거부 주장: 빠진 수치 토큰 **1개**, 다른 검색 청크에는 존재했다.
- 거부된 네 주장 모두 미상 ID는 0개다. 존재하는 청크를 잘못 연결한 오류다.
  다른 청크에 같은 숫자가 있다는 사실만으로 그 청크가 해당 주장의 의미상 정답 출처라고
  단정하지 않으며, 검색 전체 숫자로 인용 검사를 통과시키거나 ID를 자동 보완하지 않았다.

논리 비교는 **4회**, 기존 재시도를 포함한 실제 전송은 **7회**였다. 수정 사본의 긴 질문에서
서버 오류 프레임 후 재시도, 이어 출처 필드 누락 재요청이 발생했다. 기존 짧은 질문도
출처 필드 누락 재요청 1회가 있었다. 기존 제품의 복구 경로가 작동했으며 최초 실패도 기록했다.
서버 오류의 정확한 내부 원인은 확정하지 않는다. 네트워크 코드·재시도 상한은 바꾸지 않았다.
각 조건 한 번, 새 UUID·청크 배치, 재시도/생성 편차가 있어 통계적 개선이나 속도 비교는 아니다.
결과를 고르기 위한 추가 재실행도 하지 않았다.

## 검증·보호 조치와 남은 작업

- 관련 추출·검색 회귀를 새로 실행해 **130 passed**, 7.96초를 확인했다.
  이번에는 제품·테스트 코드를 변경하지 않았고 전체 API 검사는 재실행하지 않았다.
  직전 제품의 **1,549 passed / 4 skipped**는 이전 검증 결과로만 구분한다.
- 제품 `5ef7e14`가 포함된 HEAD `2e0bdff`의
  [CI 34692298914](https://github.com/chorok446/medbridge/actions/runs/34692298914)는
  이번 작업 중 **success**로 완료됐다. 이 문서 변경 이후의 CI와 구분한다.
- 첫 격리 폴더는 SQLite 파일 생성 및 조회가 권한으로 거부됐다. 그 경로를 삭제하거나
  ACL을 바꾸지 않고, 승인된 실행에서 작업 공간 아래 새 사본을 만들어 비교했다.
- 생성 문장 원문을 직접 출력하는 상세 수동 검토는 민감정보 보호 심사에서 차단됐다.
  원문 출력 없이 수치 개수·다른 청크 존재 여부만 계산하는 대안으로 진단했다.
  문장별 의미·분류 조건의 완결성 수동 평가는 **미수행**이다.
- 설치 DB의 문서 12건·페이지 4,339건·블록 364,819건·설정 1건과 대화 21/104/205행의
  작업 전후 해시가 같다. quick_check 정상, FK 위반/활성 Q&A 0건이다.
- 격리 DB와 비공개 기록은 무시된 `tmp/`에 후속 검증용으로 보관한다. Git에는 집계만 넣는다.
  [익명 측정 JSON](qa-full-section-retrieval-20260912.json).

다음 단위는 **생성 문장과 수치의 출처 연결 오류를 합성 자료에서 재현하고 인용 단위를 보강**하는
것이다. 검색량 확대만으로 해결할 문제가 아니며 기존 수치/출처 검사를 유지해야 한다.
별도로 부분 지원 결과를 완료 상태로 보여주는 한계도 남는다.
새 코드의 고정 모델 39회·설치본 GUI·전체 Windows·깨끗한 PC·대형 신규 업로드는 미검증이다.
Draft·출시 HOLD 유지, Ready·병합·릴리스·승인 파일 생성 없음.

디버깅 스킬의 실패 보존·원인 분리 원칙에 따라 순서/분리 개선과 생성·출처 실패를 구분했다.
