# 분류 답변의 출처 연결·항목 누락 진단 — 2026-09-12

> **진단과 방어 회귀 완료, 제품 동작 변경 없음.**
> 다른 청크의 숫자를 인용한 문제를 재현했고 새 회귀 8개를 추가했다.
> 전체 API **1,411 passed / 4 skipped**, Ruff·mypy 통과.
> 비교한 프롬프트 대안 3개는 모두 미채택이다. 분류 답변의 완결성은 미해결이며
> **PR Draft / 출시 HOLD**를 유지한다.

## 확인된 원인과 범위

기준 HEAD는 `5723add`이며 제품 코드는 직전 `dde86c6`과 같다.
431,634,439바이트·1,060쪽의 익명 문서 `large-1`에서
원래 이력 6/7턴의 긴/짧은 분류 질문을 유지하고 읽기 전용으로 재생했다.

1. **인용한 출처가 잘못됨:** 기준 짧은 질문은 claim 3개 중 2개가 수치 검사에서 거부됐다.
   두 claim에서 각각 중복 제외 수치 토큰 6개가 인용된 청크에 없었지만,
   그 수치들은 이미 검색된 다른 청크에 있었다. 한 claim은 바로 앞 청크의 본문을
   사용하면서 다음 청크 ID를 붙였고, 다른 claim은 여러 청크 내용을 마지막 조각 하나로
   인용했다. 검색 범위를 늘리거나 검색 전체의 숫자로 검증을 통과시킬 문제가 아니다.
2. **모델이 분류 기준을 생략함:** 기능등급 I–IV는 기준 검색 결과에 이미 포함되어 있었지만
   기준 생성 답변에는 반영되지 않았다. 후속 지침 실험에서는 생성 claim에 등장했다.
   따라서 이 관측에서 검색 부재를 직접 원인으로 볼 수 없다.
3. **생성되어도 최종 지원과는 다름:** 기능등급을 생성한 실험의 긴 질문에서는
   풀이 문장이 어휘 근거 검사에서 거부됐다. 짧은 질문에서는 4개 claim이 지원으로
   분류됐지만 최종 상충 상태가 됐다. 생성 누락, 출처 연결, 어휘 검증, 상충 판단은
   별도 단계로 추적해야 한다.

위 결과는 원문의 의학적 진위나 모든 풀이의 함의 정확성을 평가한 것이 아니다.
`not_lexically_grounded`만으로 해당 풀이가 의미상 틀렸다고 단정하지 않으며,
`supported`도 문장의 모든 세부 사실과 출처 관계가 입증됐다는 뜻은 아니다.
수치가 없는 복합 설명과 조사·표현 차이에는 현재 어휘 검사 한계가 남는다.
청크 경계 모양 자체가 ID 오류를 일으켰다는 가설도 아래 실험으로 확정하지 못했다.

## 채택하지 않은 대안

각 대안은 제품 파일이 아니라 비공개 진단 프로세스의 프롬프트 빌더만 교체했다.
기준 2회 + 대안별 2회, 총 8개 질문 재생이다. 네 묶음 모두 같은 qwen3:8b,
같은 모델 digest, 질문·이력·32개 검색 청크·기존 검증/재시도/출력 상한을 사용했다.
기능등급 이름이나 문서의 수치/정답을 지침에 직접 넣지 않았다.

| 비교 | 긴 질문 | 짧은 질문 | 미채택 이유 |
|---|---|---|---|
| 기준 | 지원 3, completed, 15.426초 | 지원 1, completed, 13.920초 | 수치 거부·기능등급 생략 재현 |
| 청크 끝에 같은 ID 경계 추가 | 지원 0, insufficient_evidence, 69.512초 | 지원 2, completed, 35.809초 | 긴 질문의 출처 형식 악화 |
| 기준별 짧은 답변·개별 출처 지침 | 지원 2, completed, 11.303초 | 지원 2, completed, 11.124초 | 양쪽 모두 어휘 거부 1개, 기능등급 미반영 |
| 유형·단계·등급 및 원문 조건 지침 | 지원 1, completed, 18.010초 | 지원 4, conflicting_evidence, 25.090초 | 긴 질문 거부 3개, 짧은 질문 최종 상충 |

- 청크 끝 표시 실험은 긴 질문에서 출처 필드 누락 재요청을 소진했다.
  모델이 ID를 본문에 붙이는 오류도 있었지만 서버가 이를 출처 배열로 추측 복원하지 않았다.
- 끝 표시 실험의 짧은 응답은 도구 출력 예산으로 진단 기록 일부가 잘렸다.
  완전한 앞부분 상태·개수·시간과 마지막 digest만 보존하며, 세부 parser/claim 분석에
  그 불완전 기록을 사용하지 않는다. 누락된 기록을 얻기 위해 결과를 다시 뽑지 않았다.
- 마지막 실험에서는 기능등급이 두 질문 모두 생성됐다. 긴 질문은 해당 claim이 어휘 검사에서
  거부됐고, 짧은 질문은 지원으로 분류됐으나 최종 상태가 상충이었다.
  지원 수만 늘었다는 이유로 성공으로 채택하지 않았다.
- 속도는 통제된 성능 측정이 아니며 모델 출력/재시도 차이도 포함한다.
- 실제 자료 원문, 생성 본문, 파일명, 내부 ID는 공개 기록에 넣지 않는다.
  [익명 진단 JSON](qa-classification-source-diagnosis-20260912.json)은 허용한 지표만 포함한다.

## 방어 회귀와 전체 검사

테스트 전용 커밋은 `f7dfb5f14e890b6ed9abe47b821de5c7ed6f09dc`다.
새 테스트는 전압/출력 기준을 가진 **합성 비의료 문서**로 다음을 두 응답 경로에서 확인한다.

- 수치가 검색된 다른 청크에 있어도 잘못 인용한 청크로 주장을 통과시키지 않는다.
- 두 청크의 내용을 인용하면서 하나만 붙인 경우 수치 누락을 차단한다.
- 정확한 단일·복수 출처는 통과하고 원문·ID·해시·좌표를 바꾸지 않는다.
- 연속된 순수 인용의 제한적 ID 복원과 혼동하지 않도록 서로 다른 쪽을 사용한다.

| 검사 | 결과 |
|---|---|
| 새 회귀 | 8 passed |
| 전체 검색 청크를 수치 근거로 쓰게 바꾼 별도 대조군 | 4 failed / 4 passed |
| 전체 API | 1,411 passed / 4 skipped, 117.38초 |
| Ruff / mypy / diff check | 통과, mypy 130개 소스 |

기존 Starlette 경고 1개는 그대로다. 대조군은 별도 프로세스의 테스트용 주입이며
제품 코드에는 적용하지 않았다. 이번 회귀는 **이미 올바르게 동작하는 수치 방어를 고정**한
테스트이지, 모델의 출처 선택 버그를 고쳤다는 증거가 아니다.

제품 프롬프트·모델 옵션·검색·검증·평가 데이터셋·게이트를 변경하지 않았다.
고정 전체 모델 평가 39회는 이번에 재실행하지 않았다.
직전 제품 `dde86c6`의 [39/39 평가](qa-evaluation-20260912-conflict-scope.json)는
그 코드의 합성 안전 평가로만 인용하고 이번 대안들의 출시 통과 결과로 쓰지 않는다.

## 데이터 보존

실제 문서 DB는 `mode=ro`와 `query_only=ON`으로 열었다. 실험 전후 문서·쪽·블록·설정과
Q&A 21/104/205행의 해시가 동일하다. quick_check 정상, FK 위반/활성 Q&A 0이다.
앱 설치·설정 변경·Ollama 설치/삭제·사용자 대화 저장·기존 문서 수정은 하지 않았다.
실험과 회귀의 원문/개인정보를 공개 커밋에 넣지 않는지 셀프 리뷰했다.

## 다음 구현 단위

프롬프트 문구만 더 바꾸는 방식은 이번 두 질문에서 안정적이지 않았다.
다음 단위는 **분류 항목별 원문 발췌와 출처를 먼저 검증하는 작은 시험 구현**이다.
제품 적용 전 아래를 고정 회귀로 검증해야 한다.

1. 검색된 분류 기준 후보와 원문 구절의 실제 청크 소속을 먼저 확인한다.
2. 표 제목과 떨어진 후속 행을 다루되 원문·ID·좌표를 추측 합성하지 않는다.
3. 검증된 발췌로만 최종 답변을 구성하고, 항목별 누락/거부를 별도로 관측한다.
4. 미상 ID, 숫자 변경, 상충, 문서 밖 풀이를 막는 기존 경계를 유지한다.
5. 추가 모델 호출이 필요한 설계라면 기존 전체 기한·취소·재시도 예산 안에서 검증한다.

이는 아직 구현 완료나 출시 승인을 뜻하지 않는다. 새 설치본 GUI·전체 Windows·깨끗한 PC·
오프라인·300MB 이상 신규 업로드/peak 메모리·문장 단위 출처 강조도 계속 남는다.
디버깅 스킬의 실패 보존·원인 분리 원칙에 따라 검증되지 않은 대안을 제품에 반영하지 않았다.
