# 인용 경계의 상충 오탐 보강 — 2026-09-12

> 제품 코드 `dde86c6`: 전체 API **1,403 passed / 4 skipped**,
> 고정 전체 로컬 모델 평가 **39/39, exit 0, default_recommended**.
> 실제 분류 답변의 오탐은 동일 답변 고정 재생으로 검증했다.
> 새 생성 답변은 각각 지원 claim 1개뿐이므로 완결성 해결은 아니다.
> **PR Draft / 출시 HOLD** 유지. 설치본 GUI 검증은 하지 않았다.

## 재현과 원인

431,634,439바이트·1,060쪽 문서 `large-1`에서 원래 이력 6/7턴을 유지한
긴/짧은 분류 질문을 각각 한 번 읽기 전용으로 재생했다.
긴 질문은 `answered` 힌트·지원 claim 3개였지만 서버가 상충으로 확정했다.
실제 의미를 확인하면 서로 다른 분류 기준과 원인 목록이지 상반된 주장이 아니었다.

기존 비교기는 claim 전체의 공통 어휘와 부정 유무를 각각 합쳤다.
설명과 인용의 서로 다른 문맥이 섞이고 연결어 및 조사만 붙은 반복 주제어가
별도 주제로 집계되어 최소 공통 어휘 3개를 채웠다.
반대로 무관한 부정 인용을 덧붙이면 양쪽 claim 모두 부정으로 취급되어
실제 상충을 놓치는 경우도 합성 회귀에서 재현했다.

초기 인용 경계/연결어 수정만으로는 실제 고정 답변의 오탐이 남았다.
반복 주제어 중복 집계를 합성 예제에 추가하자 4개 회귀가 실패했으며,
중복 집계까지 보강한 뒤 고정 답변도 정상 완료로 바뀌었다.
실제 자료의 원문·파일명·내부 ID는 테스트나 공개 기록에 복사하지 않았다.

## 최소 변경과 셀프 리뷰

- `원문·인용·출처·original·source·quote` 도입부를 경계로 나눈 구간끼리 비교한다.
  설명과 인용을 교차 비교하며 인용 본문을 버리지 않는다. 일반 괄호 안 용어도 유지한다.
- `따라·따른·according·to·is·are`는 주제 비교용 공통 어휘에서 제외한다.
  두 구간의 공통 어휘에 기본형과 `은/는` 형태가 모두 있을 때만 중복을 한 번으로 센다.
  한쪽에만 기본형이 있다고 공유 토큰을 잃거나 모든 어미를 일괄 제거하지 않는다.
- 최소 공통 주제 수 3, 출처 ID·수치·어휘·출처 극성 검증, 명시적 상충 힌트와
  지원 근거 2개 미만의 보류 조건은 유지한다. 프롬프트·검색·재시도·평가 기준은 불변이다.
- 제품 파일 1개와 회귀 테스트 2개만 변경했다. DB·프런트·설치기·의존성 변경은 없다.
- 분류 질문 전체의 상충 검사를 끄거나 문장을 강제로 지원 상태로 만드는 대안은 쓰지 않았다.
  일반 괄호를 모두 분리하면 문장 안의 대상 어휘가 끊기므로 적용하지 않았다.
- **한계:** 인용 도입부 기준의 어휘/부정 휴리스틱이다. 닫는 괄호 이후의 문장을
  완전하게 구문 분석하지 않으며, 무표지 인용·복합 절·반의어·동일 claim 안의 모순을
  일반적으로 판정하는 의미 검증기는 아니다. 새로운 구간 단위 판정의 오탐/미탐은 남는다.

## 회귀와 전체 검증

| 검사 | 결과 |
|---|---|
| 첫 합성 단위 재현 | 15 failed / 8 passed |
| 반복 주제어 추가 후 중간 재현 | 4 failed / 19 passed |
| 최종 새 단위/API 회귀 | 27 passed |
| 기존 비교기를 주입한 별도 API 대조군 | 2 failed / 1 passed |
| 전체 API | 1,403 passed / 4 skipped, 122.41초 |
| Ruff / mypy / diff check | 통과, mypy 130개 소스 |

API 회귀는 합성 PDF 업로드·실제 검색·모의 공급자·최종 상태·claim/인용/DB 저장·draft
정리를 검사한다. 기존 비교기 대조군은 분류 오탐과 실제 상충 미탐을 각각 잡았다.
새 로직 재활성화 후 모두 통과했다. 기존 Starlette 경고 1개는 그대로이며,
검증 중 새 코드의 줄 길이와 import 서식 2건을 수정한 후 재검사했다.
디버깅 스킬의 재현→최소 수정→회귀→전체 검증 순서로 단일 목적을 유지했다.

## 실제 문서 재생과 남은 답변 누락

| 실행 | 지원 claim | 최종 상태 | 거부 사유 |
|---|---:|---|---|
| 수정 전 긴 질문, 15.196초 | 3 | conflicting_evidence | 없음 |
| 수정 전 짧은 질문, 14.610초 | 1 | completed | 수치 2개 |
| 같은 긴 답변을 고정해 새 최종화 적용 | 3 | completed | 재생성·근거 재작성 없음 |
| 같은 짧은 답변을 고정해 새 최종화 적용 | 1 | completed | 기존 거부 유지 |
| 수정 후 긴 질문 새 생성, 26.136초 | 1 | completed | 어휘 1개, 수치 1개 |
| 수정 후 짧은 질문 새 생성, 14.714초 | 1 | completed | 수치 2개 |

새 생성 답변 두 건의 완료 상태만으로 오탐 수정이나 완결성을 입증하지 않는다.
오탐 수정의 분리된 증거는 **동일한 지원 claim 3개를 유지한 고정 답변 최종화**다.
긴 질문의 새 생성 첫 전송은 `connect_failed`로 claim 3개를 버린 뒤 기존 재시도에서
복구했다(총 2회). 답변 지연은 성능 벤치마크가 아니다.

**표 누락 진단 정정:** 처음에는 `NYHA` 표지가 들어 있는 청크만 보고 제목과 첫 행만
검색됐다고 설명했다. 후속 인접 조각 검사에서 **II–IV 행도 이미 검색됨**을 확인했다.
제목 청크로부터 읽기 순서 +8인 213자 조각에 후속 행이 들어 있고,
사이에 다른 내용이 끼어 있다. 전체 모델 입력은 32청크·2,250자였다.
따라서 단순한 검색 누락/문자 상한 초과라고 단정할 수 없다.
검색에는 기능등급 근거가 있으나 이번 생성 claim에는 반영되지 않았다는 것이 확인된 범위다.

읽기 전용 SQLite `mode=ro`와 `query_only=ON`을 함께 적용했다.
전후 문서 12개·쪽 4,339개·블록 364,819개·설정 1개와
현재 Q&A 21/104/205행의 해시가 같고 quick_check 정상, FK 위반/활성 Q&A 0이다.
기존 백업의 문서·설정 및 원래 대화 행도 보존되었다.
[익명 재생·표 선택·보존 지표](qa-conflict-scope-20260912.json)에 원문 없이 기록했다.

## 고정 전체 로컬 모델 평가

```powershell
.\apps\api\.venv\Scripts\python.exe -X utf8 apps/api/scripts/evaluate_local_qa.py --model qwen3:8b --repeat 3 --fail-on-gate --keep-failed-artifacts --output-dir artifacts/qa-evaluation/20260912-conflict-scope
```

- clean tree `dde86c6f930af95f21de4b16f6f7811937633d83`, 기존 전체 13사례 × 3회.
  계측 래퍼나 카테고리 필터 없이 첫 전체 실행을 기록했다. 평가/기대값 변경이나 반복 보정 없음.
- **39/39, exit 0, default_recommended**, 모든 카테고리 통과율 100%.
  안전 실패·명시적 위반·중요 사례 실패·변동 0, 상충 사례 3/3 conflicting_evidence.
- qwen3:8b의 실행 전후 고정 digest:
  `sha256:500a1f067a9f782620b40bee6f7b0c89e17ae61f686b92c24933e4ca4b2b8b41`.
- p50 2.329초 / p95 22.717초. 기존 HTTP 500 재시도 두 건은 각 두 번째 시도에서 복구했다.
- 사용자 DB가 아닌 합성 임시 DB와 in-memory 자격증명 저장소에서 평가했다.
  합성 DB·원본 보고서를 보존하고 [전체 평가 JSON](qa-evaluation-20260912-conflict-scope.json)을
  같은 JSON 내용으로 공개했다. 이전 38/39 실패 기록과 직전 39/39 기록도 유지한다.
- 고정 합성 평가의 통과는 실제 자료 답변의 완결성이나 일반 의학적 정확성 보장이 아니다.

## CI와 다음 작업

- 직전 `bcd4394`의 [CI 34685754292](https://github.com/chorok446/medbridge/actions/runs/34685754292)는
  Windows 빌드를 포함한 6개 job 모두 성공이다. 이번 커밋의 CI 결과와 구분한다.
- 우선순위 1: 이미 검색된 분류 기준이 생성 답변에서 빠지는 경로를 고정 재현한다.
  제목/후속 행 사이의 다른 내용, 모델의 답변 선택, 실제 인용 ID와 수치 거부를 분리 진단한다.
  확인 없이 검색 상한을 늘리거나 수치 검증을 완화하지 않는다.
- 우선순위 2: 새 CI 설치본으로 분류 답변·출처 이동을 GUI 검증한다.
  전체 Windows·깨끗한 PC·오프라인·300MB 이상 신규 업로드/peak 메모리와
  문장 단위 출처 강조는 계속 남아 있다.
- 이번 작업에는 앱 설치/설정 변경, Ollama 설치/삭제, 사용자 문서/대화 쓰기가 없다.
  PR Draft/HOLD 유지, Ready·병합·릴리스·승인 파일 생성 없음.
