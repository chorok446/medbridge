# PR7 검색 보강 설치본 검증 — 2026-09-10

> **출시 HOLD / PR Draft 유지**. 업데이트·기존 데이터 보존·정상 재실행은 통과했다.
> 431MB 문서의 정의 질문은 첫 회 보류, 재질문은 목차 제목만 답하고 완료됐다.
> 출처 이동 성공과 `completed/supported`는 질문에 대한 답변 성공을 뜻하지 않는다.

## 설치본 식별과 안전한 업데이트

- [CI 34396816569](https://github.com/chorok446/medbridge/actions/runs/34396816569)의
  frontend/backend/versions/security/windows-build/summary 6개 job이 성공했다.
- PR head: `324e9fda6b3c876d7f513def00889ed4b0c19e6e`.
  manifest의 testedCommit `41d04739f24e7f9a5d89a8f6798fb8ce2f96ca35`는 GitHub의
  임시 PR merge commit이다. 부모에 PR head가 포함되며 체크아웃 로그와 대조했다.
  로컬 HEAD와 동일한 tree `69ad06b7b321c5f8e19253c801583c7721f0e0f3`다.
  PR을 실제로 병합한 것은 아니다.
- artifact `10122460774` / `medbridge-windows-setup`의 installer와 합성 PDF 7개를
  manifest의 크기·SHA-256과 대조했다. installer는 105,749,825 bytes, NotSigned다.
  SHA-256: `f616247b44e55d93842891e51ee1bab88fc2a9dbd275aa9a420e24bc5831a942`.
- 앱·sidecar가 종료된 상태에서 설치 폴더와 앱 데이터를 새 위치에 백업했다.
  93파일 / 9,210,190,648 bytes, 원본의 복사 전후·백업 해시가 모두 일치했다.
  21:07:52 UTC에 다시 93파일을 대조한 뒤 사용자의 설치 직전 승인으로 진행했다.
- Computer Use로 기존 경로의 **컴포넌트 추가 및 재설치**를 선택했다.
  제거·초기화·보안 설정 변경은 하지 않았다. 새 바탕화면 바로가기는 해제하고
  앱 실행을 선택했다. 정상 설치 완료와 기존 경로의 실행 프로세스를 확인했다.
  깨끗한 PC 최초 설치 검증은 아니다. 기존 백업·Ollama도 보존했다.
- 설치 후 medbridge.exe SHA-256:
  `6daa98ee0f228d13a6c5e00a92db823dd51038d4c619b052ac85b7ea930d8cb7`.
  sidecar SHA-256:
  `bbd40f3e8c382cc519d013d25233a50daf27a11c554998db5bbe3a68170ff61b`.
  모두 이전 `49131af` 바이너리와 다르다. 표시 버전 0.1.0만으로 구분하지 않았다.

## 실제 GUI: 같은 새 대화에서 정의 질문 2회

환경은 Windows 11 / RAM 31.1 GiB / RTX 3080, 기존 local Ollama 0.33.3와
qwen3:8b다. 이전 오답 이력과 분리한 새 대화에서 원래 질문을 그대로 두 번 보냈다.
아래 시각은 2026-09-09 UTC이며 현지 실행일은 2026-09-10 KST다.

질문: “심부전의 정의를 설명하는 원문 한 문장과 출처를 보여주세요.”

`large-1`: 431,634,439 bytes / 1,060쪽 / 활성 청크 16,239개,
content/chunk revision 17. 기존 PARTIALLY_EXTRACTED 자료를 재처리하지 않았다.

| 실행 | DB 결과 | 화면·원문 검토 판정 |
|---|---|---|
| 21:12:25 → 21:12:39 | insufficient_evidence, 14.481초, claim 0 | 정의가 있지만 답변 보류: 실패 |
| 21:13:42 → 21:13:48 | completed, 6.119초, supported 1개 | 정의 대신 목차 제목만 제시: 실패 |

두 번째 claim은 17자이며 출처는 PDF 104쪽의 목차 항목이다. 실제 출처 버튼을 누르면
104/1060쪽으로 이동하고 해당 제목이 강조됐다. DB 블록·좌표도 일치한다.
그러나 요청한 **정의 문장이 전혀 없는 비답변**이다. 이전 호흡부전 오답과는 다른 실패다.
두 번에서 같은 질환 혼동이 안 나왔다고 기존 오답의 완전 해결을 선언하지 않는다.
답변은 새 대화 1개·메시지 4개·claim 1개로 남겼고 삭제하지 않았다.
사용자 자료명·본문·답변 원문·화면은 저장소에 공개하지 않는다.

## 읽기 전용 출처 연결 진단: 중간 조각 하나만 인용

설치본 GUI와 별도로 같은 제품 코드의 검색 → 로컬 모델 스트리밍 → claim 검증을
3회 재생했다. 사용자 DB는 `mode=ro`/`query_only=ON`이며 저장·이전 대화 경로는 없다.
GUI 첫 번째 보류의 내부 이벤트를 직접 계측한 결과로 해석하지 않는다.

- 32청크 / 2,090자 문맥에 138쪽 정의의 세 조각이 포함됐다.
  0-based 문맥 위치 5/6/7, 각각 45/47/280자다. 마지막 조각에는 뒤 표도 포함된다.
- 세 실행 모두 같은 246자 claim을 각각 두 번 방출했고 `answered` 힌트를 보냈다.
  하지만 인용한 sourceChunkIds는 매번 **문맥 위치 6의 47자 중간 조각 하나**뿐이다.
  정의 시작·끝 조각을 인용하지 않아 전체 claim 어휘 중복과 부정 극성 검사가 실패했다.
  6개 이벤트 모두 `not_lexically_grounded`로 거부돼 최종 insufficient_evidence다.
  실행 시간은 7.329 / 7.026 / 7.034초. 반복은 탐색 재현이며 성능 벤치가 아니다.
- 모델 시작·종료 digest:
  `sha256:500a1f067a9f782620b40bee6f7b0c89e17ae61f686b92c24933e4ca4b2b8b41`.
- 검색이 정의를 가져오는 것과 모델이 필요한 모든 출처 ID를 연결하는 것은 별개다.
  이번 진단은 인용 누락을 좁혔으며 검증 임계치를 낮출 근거가 아니다.
  목차 제목만으로 완료되는 GUI 실패에는 질문 충족 여부도 별도로 다뤄야 한다.

## 보존·재실행과 검증 범위

설치 직후와 질문 후, 정상 종료·재실행 후 모두 읽기 전용으로 검사했다.

- 기존 documents 12 / pages 4,339 / blocks 364,819 / settings 1행의 내용 해시가
  새 백업과 같다. 이전 Q&A threads 19/19, messages 90/90, claims 201/201행도 같다.
- 정상 종료 시 앱·sidecar가 사라졌다. 재실행 전후 새 Q&A 총 20/94/202행의
  해시가 같으며 내 학습자료 화면이 정상 로딩됐다.
  quick_check=ok, FK 위반 0, active Q&A 0이다.
- [바이너리·CI·익명 GUI 결과·출처 진단·보존 JSON](windows-pr7-validation-20260910-324e9fd.json).
- 이번에는 **제품 코드·모델·평가 기준을 바꾸지 않았다**. 기존 고정 `74b9925`의
  [39/39 합성 평가](qa-evaluation-20260910-retrieval.json)와 설치본의 제품 코드가 같지만,
  그 통과가 실제 대형 문서 실패를 대신하지 않는다. 전체 모델 평가를 재실행한 것은 아니다.

## 후속 수정의 경계

1. 문장 단위의 인용 범위를 원래 청크·블록에 연결하는 입력 구성을 별도 회귀로 검증한다.
   다른 청크 본문을 한 ID에 붙이거나 검증 기준을 완화하지 않는다.
2. 정의 요청에 목차·제목만 제시해 완료되는 경우를 합성 회귀로 만든다.
   검색의 목차 처리와 답변 충족 판정을 나눠 최소 변경을 결정한다.
3. 코드 변경 후 고정 커밋 모델 평가·CI·같은 실제 문서 GUI를 다시 검증한다.
   깨끗한 PC·오프라인·Ollama 중단/복구·전체 Windows 항목·대형 업로드 및 peak 메모리는
   아직 완료되지 않았다. 이 문서 전용 커밋 때문에 설치본을 다시 교체할 필요는 없다.
