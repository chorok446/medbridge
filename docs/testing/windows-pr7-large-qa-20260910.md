# PR7 연속 질문 및 대형 문서 검색 검증 — 2026-09-10

> **출시 HOLD / PR Draft 유지**. 설치본의 합성 문서 연속 질문 10회는 통과했다.
> 실제 대형 문서에서는 심부전을 호흡부전으로 설명하는 오답을 발견했다.
> `d977f28`로 검색·문맥 수집을 보강했으나, 수정 후 로컬 진단 3회도 답변 보류였다.
> 대형 문서 Q&A 완료나 의료적 정확성 통과로 판정하지 않는다.

## 실행 환경과 범위

- 기존 `49131af` 설치본, Windows 11 / RAM 31.1 GiB / RTX 3080,
  Ollama 0.33.3 / local qwen3:8b. 상세 바이너리·백업 해시는
  [직전 설치본 보고서](windows-pr7-validation-20260910-49131af.md)에 있다.
- GUI 질문은 Computer Use로 실행했다. DB 진단은 `mode=ro`와 `query_only=ON`이다.
  사용자 PDF·DB·설정·Ollama를 삭제하거나 재추출·청크 재생성하지 않았다.
- 아래 시각은 2026-09-09 UTC이며 실제 실행일은 2026-09-10 KST다.
  수정 코드는 아직 설치하지 않았다. 이번에 installer를 다시 실행하지 않았다.

## 설치본: 합성 문서 연속 질문 10회 통과

같은 2쪽 합성 PDF에서 새 대화를 만들고 같은 짧은 질문을 순차적으로 10번 보냈다.
질문은 “이 문서에서 심장은 어떤 기관이라고 설명하나요?”다.

- 18:50:40~19:07:54, **10/10 completed**, 각 supported claim 1개,
  원문과 일치하는 답변·출처 1/2쪽·중복 0개. 각 완료 경계에서 active Q&A 0개.
- 응답 시간 3.270~30.405초, 중앙값 24.680초. cold/warm 구분 계측은 아니다.
- 앱·sidecar·WebView2 프로세스 트리의 완료 경계 스냅샷:
  working set 582.8~592.3 MiB, private 370.7~400.1 MiB.
  첫 완료 대비 마지막 완료 증가는 각각 약 8.1 / 1.0 MiB다.
  연속 peak·Ollama·VRAM 측정이나 장기 메모리 누수 입증으로 해석하지 않는다.
- 체크리스트 29번은 **이 합성 질문 시나리오에서 통과**다.
  다양한 질문 품질, 대형 문서 10회 연속, 전체 36항목 통과는 아니다.
- [질문별 상태·시간·보존·메모리 JSON](windows-pr7-sequential-20260910-49131af.json),
  [10번째 완료 화면](screenshots/pr7-49131af-sequential-10.jpg).

## 설치본: 실제 대형 문서 Q&A 실패

`large-1`: 431,634,439 bytes / 1,060쪽 / 활성 청크 16,239개,
content/chunk revision 17. PARTIALLY_EXTRACTED 상태의 기존 자료다.
1,059쪽에 텍스트가 있으며 저신뢰 OCR 등 잔여 페이지가 있다.
312,729,344-byte의 다른 문서는 청크가 0개여서 이번 Q&A 대상에 넣지 않았다.

질문: “심부전의 정의를 설명하는 원문 한 문장과 출처를 보여주세요.”

| 같은 새 대화의 질문 | DB 결과 | 실제 판정 |
|---|---|---|
| 첫 질문, 19:11:09 | insufficient_evidence, 4.749초, claim 0 | 원문에 정의가 있으나 답변 못함 |
| 두 번째 질문, 19:13:10 | completed, 8.919초, supported 2개, 출처 20쪽 | 심부전과 호흡부전을 혼동한 오답 |

두 번째 출처의 블록·좌표는 DB와 일치했다. **출처 좌표 일치와 supported 상태가
질문의 주제·의학적 의미를 입증하지 않는 실제 실패**다. 화면에서 답변 내용을 읽고
확인했다. 실제 문서명·본문·답변 원문·화면은 저장소에 공개하지 않는다.

동일 질문을 기존 검색 코드로 읽기 전용 재생하면 280/283/95/718/20/422/916/3쪽
8개 청크를 선택한다. 모두 질문의 “한”만 일치하고 나머지 어절은 일치하지 않는다.
짧은 어절의 LIKE 폴백과 OR 빈도 순위, 조사 미처리가 결합한 문제다.
실제 정의는 138쪽에서 세 청크로 나뉘어 있어 검색어만 고쳐도 문장 전체가 빠질 수 있다.

## 검색·문맥 수집 수정 — d977f28

- Q&A 검색어에서 제한된 조사와 요청 표현을 정리한다. 주제가 따로 있으면
  “정의/진단 기준” 같은 공통 표현만으로 다른 질환을 검색하지 않도록 제외한다.
  원래 모델 질문, 일반 검색 API의 AND 의미, 수치·약어·부정 표현은 유지한다.
- 정확한 주제 제목을 우선하고 같은 실제 출처 페이지의 가까운 청크를 함께 읽는다.
  앵커 8개, 청크 인덱스 -3~+8 범위, 총 32개·12,000자·청크당 3,000자 상한이다.
  중복 청크의 넓은 page_start/end 범위를 인접성 근거로 쓰지 않는다.
- 각각의 원래 chunk ID·본문·출처·해시를 유지한다. 활성 세대 필터도 유지하며
  다른 청크의 본문을 하나의 ID에 붙이거나 근거 검증 임계치를 낮추지 않는다.
- 새 회귀 25개(검색 통합 11개, 검색어 단위 14개). 최초 재현 9개 중 수정 전
  7개 실패/2개 통과를 확인했다. 전체 테스트에서 추가 발견한 요청 표현·공통 기준
  검색 회귀를 보완했고, 기존 자료 부재 평가 기준은 변경하지 않았다.
- 최종 API **1,210 passed / 4 skipped**, 118.44초, 기존 Starlette 경고 1개.
  Ruff 전체 및 mypy 128개 소스 통과, `git diff --check` 통과.

수정 후 동일 대형 문서의 읽기 전용 검색은 32청크 / 2,090자를 수집했고,
138쪽의 정의 문장 조각 세 개가 포함됐다. 한국어 형태소·의미 검색 전체를
해결한 것은 아니다. 새 의존성·마이그레이션·전체 문서 재처리는 없다.

## 수정 후 로컬 모델 진단: 3회 모두 보류

커밋 전 동일 코드에서 검색 → 기존 스트리밍 공급자 → claim 검증 → 최종 상태 계산을
재생했다. **GUI·대화 저장·revision 재확인 전체 경로 평가가 아닌 탐색 진단**이다.
기존 로컬 Ollama만 사용하고 사용자 DB에는 쓰지 않았다. 시작·종료 모델 digest는 같다.

- 첫 실행: 4.760초, claim/final 힌트 없이 insufficient_evidence.
- 두 번째/세 번째: 6.753 / 6.831초, 모델 final은 answered였지만
  각각 claim 2개가 `not_lexically_grounded`로 거부돼 insufficient_evidence.
- 출력 내용은 정의 문장 쪽으로 바뀌었지만 완전한 출처 연결·검증 통과는 미완료다.
  이 세 번의 오답 미노출을 기존 오답의 완전 해결이나 전체 안전성 통과로 확대하지 않는다.
- [설치본 실패·검색 전후·로컬 모델 진단·보존 메타데이터](windows-pr7-large-qa-20260910.json).

## 고정 커밋 전체 실제 모델 평가 — 74b9925

검색 구현 `d977f28`와 문서 기록을 포함한 `74b99250fd3104b78c8d30508f508f7d5a39ce59`의
clean tree에서 별도 평가 DB·in-memory keyring을 사용했다. 시작·종료 Git과 모델
digest를 기존 fail-closed 검사로 고정했으며 평가 기준·데이터셋은 바꾸지 않았다.

```powershell
# apps/api
.\.venv\Scripts\python.exe -X utf8 scripts/evaluate_local_qa.py --model qwen3:8b --repeat 3 --fail-on-gate --output-dir artifacts/qa-evaluation/d977f28-retrieval-context
```

- **13케이스 × 3회 = 39/39 통과**, exit 0, `default_recommended`.
  평가 기준상 위해 노출·안전 중요 실패·변동 0건, p50 1.850초 / p95 19.682초.
  생성 시각 19:41:20 UTC. 검색 → 스트리밍 → 검증 → 저장 전체 서비스 경로 평가다.
- 모델 digest:
  `sha256:500a1f067a9f782620b40bee6f7b0c89e17ae61f686b92c24933e4ca4b2b8b41`.
- [전체 평가 JSON](qa-evaluation-20260910-retrieval.json).
  실행 원본 SHA-256: `872db6bd46aa03e749fab7db31a1d647bdf1c303b28b4ae3706af9ac81c31171`.
  저장소 사본은 LF 정규화 후 내용 일치를 확인했다.
- **합성 평가 통과는 위 실제 431MB 문서의 답변 보류나 이전 오답을 해소하지 않는다.**
  설치본 교체·대형 문서 의미 정확성 검증이 남아 전체 출시 판정은 HOLD다.

## 보존 및 다음 작업

검증 전후 기존 documents/pages/blocks/settings와 이전 Q&A 3개 테이블의
행 수·내용 해시가 같다. 기존 문서 잡·요약 상태 집계도 같고 active Q&A/FK 위반은 0이다.
새 합성 질문 10회와 대형 문서 질문 2회만 설치본에 남았다. 백업은 유지했다.

1. 새 CI 결과와 installer를 확인한다. 위 고정 커밋 평가는 CI·GUI 검증과 구분한다.
2. 분절된 문장의 claim/sourceChunkIds 연결 실패를 별도 재현한다. 검증 기준을
   완화하지 않고 출처를 보존하는 입력·인용 구성 개선을 작은 변경으로 검토한다.
3. 새 설치본에서 대형 문서 질문·출처 이동·보류 결과를 다시 확인한다.
   깨끗한 PC, 오프라인, Ollama 중단/복구, 대형 문서 처리 부하·메모리 검증도 남는다.
